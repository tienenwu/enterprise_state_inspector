name: enterprise_state_inspector_example
description: Example application demonstrating enterprise_state_inspector usage.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.3.2
  flutter_bloc: ^8.1.3
  get: ^4.6.5
  enterprise_state_inspector:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
